<UserControl x:Class="Shibaura_ControlHub.Views.SwitcherViewB"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:conv="clr-namespace:Shibaura_ControlHub.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="1280" d:DesignWidth="1920">
    <UserControl.Resources>
        <conv:IntEqualityConverter x:Key="IntEqualityConverter"/>
        <conv:PresetStateConverter x:Key="PresetStateConverter"/>
        <!-- スイッチャープリセットボタン選択状態用スタイル -->
        <Style x:Key="SwitcherPresetButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="MaxHeight" Value="32"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                Height="32"
                                MinHeight="32"
                                MaxHeight="32">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#2D2D30"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- 選択されているが呼び出されていない（オレンジ） -->
                <DataTrigger Value="1">
                    <DataTrigger.Binding>
                        <MultiBinding Converter="{StaticResource PresetStateConverter}">
                            <Binding Path="SelectedSwitcherPresetNumber"/>
                            <Binding Path="CalledSwitcherPresetNumber"/>
                            <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                        </MultiBinding>
                    </DataTrigger.Binding>
                    <Setter Property="Background" Value="#F59E0B"/>
                    <Setter Property="Foreground" Value="White"/>
                </DataTrigger>
                <!-- 選択されていて呼び出し済み（濃い青） -->
                <DataTrigger Value="2">
                    <DataTrigger.Binding>
                        <MultiBinding Converter="{StaticResource PresetStateConverter}">
                            <Binding Path="SelectedSwitcherPresetNumber"/>
                            <Binding Path="CalledSwitcherPresetNumber"/>
                            <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                        </MultiBinding>
                    </DataTrigger.Binding>
                    <Setter Property="Background" Value="{StaticResource CalledPresetBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <StackPanel Margin="12">
        <TextBlock Text="映像切替" Style="{StaticResource PageTitleStyle}"/>

        <!-- プリセットセクション -->
        <StackPanel  Margin="0,0,0,20">
            <TextBlock Text="プリセット" Style="{StaticResource SectionHeaderStyle}"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- プリセット選択ボタン -->
                <UniformGrid Grid.Column="0" Columns="2" Margin="0,0,10,0">
                    <Button Content="1" Tag="1"
                            Command="{Binding SelectSwitcherPresetCommand}"
                            CommandParameter="1"
                            Style="{StaticResource SwitcherPresetButtonStyle}" Margin="2"/>
                    <Button Content="2" Tag="2"
                            Command="{Binding SelectSwitcherPresetCommand}"
                            CommandParameter="2"
                            Style="{StaticResource SwitcherPresetButtonStyle}" Margin="2"/>
                    <Button Content="3" Tag="3"
                            Command="{Binding SelectSwitcherPresetCommand}"
                            CommandParameter="3"
                            Style="{StaticResource SwitcherPresetButtonStyle}" Margin="2"/>
                    <Button Content="4" Tag="4"
                            Command="{Binding SelectSwitcherPresetCommand}"
                            CommandParameter="4"
                            Style="{StaticResource SwitcherPresetButtonStyle}" Margin="2"/>
                    <Button Content="5" Tag="5"
                            Command="{Binding SelectSwitcherPresetCommand}"
                            CommandParameter="5"
                            Style="{StaticResource SwitcherPresetButtonStyle}" Margin="2"/>
                    <Button Content="6" Tag="6"
                            Command="{Binding SelectSwitcherPresetCommand}"
                            CommandParameter="6"
                            Style="{StaticResource SwitcherPresetButtonStyle}" Margin="2"/>
                    <Button Content="7" Tag="7"
                            Command="{Binding SelectSwitcherPresetCommand}"
                            CommandParameter="7"
                            Style="{StaticResource SwitcherPresetButtonStyle}" Margin="2"/>
                    <Button Content="8" Tag="8"
                            Command="{Binding SelectSwitcherPresetCommand}"
                            CommandParameter="8"
                            Style="{StaticResource SwitcherPresetButtonStyle}" Margin="2"/>
                </UniformGrid>

                <!-- 呼出ボタン -->
                <Grid Grid.Column="1">
                    <Button Content="呼出" FontSize="12" FontWeight="Bold" Style="{StaticResource ModernButtonStyle}" 
                            Command="{Binding CallSwitcherPresetCommand}" 
                            Padding="20,12" Margin="2"/>
                </Grid>
            </Grid>
        </StackPanel>

        <!-- 詳細設定セクション -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="詳細設定" FontSize="14" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,10"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 列ヘッダー -->
                <ItemsControl Grid.Row="0" Grid.Column="1" ItemsSource="{Binding SwitcherMatrixColumnLabels}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="1" Columns="8"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="9" FontWeight="SemiBold" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       TextWrapping="Wrap" TextAlignment="Center"
                                       Foreground="{StaticResource SubTextBrush}"
                                       Margin="2">
                            </TextBlock>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- 行ヘッダー -->
                <ItemsControl Grid.Row="1" Grid.Column="0" ItemsSource="{Binding SwitcherMatrixRowLabels}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="9" Columns="1"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="10" FontWeight="SemiBold" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       TextWrapping="Wrap" TextAlignment="Center"
                                       Foreground="{StaticResource SubTextBrush}"
                                       Margin="2">
                            </TextBlock>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- マトリクス本体 -->
                <ItemsControl Grid.Row="1" Grid.Column="1" ItemsSource="{Binding SwitcherMatrixButtons}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="9" Columns="8"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="" 
                                    Command="{Binding DataContext.SelectSwitcherMatrixCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Padding="8,8" 
                                    Margin="1"
                                    IsManipulationEnabled="False">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
                                        <Setter Property="Stylus.IsTapFeedbackEnabled" Value="False"/>
                                        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Viewbox Stretch="Uniform" StretchDirection="Both">
                                                    <Border x:Name="border"
                                                            Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="0"
                                                                Padding="{TemplateBinding Padding}"
                                                                Width="100"
                                                                Height="100">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    </Viewbox>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Background" Value="{StaticResource ControlBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
            </Grid>
        </StackPanel>
</UserControl>
