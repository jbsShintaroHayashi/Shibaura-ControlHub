<UserControl x:Class="Shibaura_ControlHub.Views.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1280" d:DesignWidth="1920"
             Background="{StaticResource BackgroundBrush}">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="600">
            <TextBlock Text="{Binding Message}" FontSize="24" Foreground="{StaticResource PrimaryBrush}"
                       TextAlignment="Center" Margin="0,0,0,30"/>
            <ProgressBar IsIndeterminate="{Binding IsIndeterminate}"
                         Minimum="0" Maximum="100"
                         Value="{Binding ProgressValue}"
                         Height="20"/>
        </StackPanel>
    </Grid>
</UserControl>

