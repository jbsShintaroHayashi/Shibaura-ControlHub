<UserControl x:Class="Shibaura_ControlHub.Views.PowerOnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1280" d:DesignWidth="1920"
             Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.ColumnSpan="2" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="600">
            <TextBlock Text="タブレットまたはスイッチパネルによりシステム電源をオンにしてください"
                       FontSize="18" TextAlignment="Center" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,30"/>
            <Button Content="システム電源オン"
                    Command="{Binding PowerOnCommand}"
                    HorizontalAlignment="Center"
                    Width="260" Height="80"
                    FontSize="16"
                    Style="{StaticResource ModernButtonStyle}"
                    Padding="20,12"/>
        </StackPanel>
    </Grid>
</UserControl>

